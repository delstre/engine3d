Подготовка проекта
0. Обособить исполняемый файл от проекта, сделать ProjectPath чтобы бралось все только из ProjectPath.
1. Убрать resently_used.json, сделать открытие через main (к примеру ./wsm $PATH (путь до проекта))
2. Сделать версионирование библиотеки для обновления библиотеки движка
3. Необходимо сделать начальный интерфейс движка
4. Описание каждой функции (что, зачем, почему!)

Работа в этом семетре
1. Raycast, выделение полигонов, выделение обьекта
2. Hotkey для перетаскивания полигонов, обьектов
3. Hotkey для создания и изменения точек для полигонов
4. Добавить базу данных SQL ...


build/*.o
+x ./wsm

ProjectPath

x engine3d/models
| engine3d/shaders
| engine3d/build/libengine.so
| engine3d/include
| create Makefile
-> new_project/*

Простой 3D движок с использованием:
  - OpenGL
  - GLEW
  - ImGUI
